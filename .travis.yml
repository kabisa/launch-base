sudo: required
matrix:
  include:
    - language: ruby
      before_install:
        - gem install bundler
      install:
        - bin/setup
      script:
        - bin/ci
    - language: node_js
      node_js:
        - "8"
      script:
        -  npm run test-eslint
