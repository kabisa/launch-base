{
  "name": "<%= @app_id %>",
  "description": "<%= @app_description %>",
  "keywords": [
    "dokku",
    "rails",
    "<%= @app_id %>"
  ],
  "scripts": {
    "dokku": {
      "predeploy": "echo $RAILS_ENV",
      "postdeploy": "bin/post_deploy"
    }
  }
}
